يوضح الفيديو عملية الوصول إلى بوابة إدارة مستخدمي خدمة إنترنت تسمى "SAS RADIUS" عبر متصفح الهاتف المحمول، مع انتقال سلس نحو تسجيل الدخول التلقائي وعرض تفاصيل الحساب.

### تحليل الخطوات الزمنية والواجهة:

1.  **00:00 - 00:04 (بدء التشغيل والملاحة):**
    *   يبدأ الفيديو بفتح متصفح Chrome.
    *   يتم إدخال أو اختيار عنوان URL: `http://admin.skylineiq.com/user/`.
    *   تظهر اقتراحات البحث وسجل التصفح الذي يحتوي على روابط سابقة لنفس النطاق.

2.  **00:05 - 00:06 (صفحة تسجيل الدخول والتحميل التلقائي):**
    *   تظهر صفحة تسجيل دخول بعنوان **"SAS RADIUS"**.
    *   **الحقول المرئية:**
        *   حقل "اسم المستخدم أو رقم الهاتف" (username or mobile number).
        *   حقل "كلمة المرور" (your password).
        *   قائمة اختيار اللغة (مضبوطة على English).
    *   **الأزرار:** "Login" (أزرق)، "Forgot password?"، وزر "Register" في الأسفل.
    *   **ملاحظة تقنية:** يظهر مؤشر تحميل (Spinner) للحظة فوق الحقول، مما يشير إلى أن المتصفح يقوم بتعبئة البيانات المحفوظة تلقائياً وإرسال الطلب (Auto-login) دون تدخل يدوي في هذه اللحظة.

3.  **00:07 - 00:12 (لوحة تحكم المستخدم - User Portal):**
    *   يتم توجيه المستخدم بنجاح إلى "User Portal".
    *   **البيانات المعروضة:**
        *   **ملخص الحساب:** الأيام المتبقية (0 يوم)، الرصيد (IQD 0.00)، الفواتير غير المدفوعة (0).
        *   **معلومات العميل:** معرف المستخدم (300285)، الاسم (استاذ طه جديد)، واسم المستخدم (`bbn.17@haq2`).
        *   **معلومات الخدمة:** نوع الخدمة (2-Nitro)، حالة الاشتراك (Active)، تاريخ الانتهاء (2026-09-13)، والحالة (ONLINE).
    *   **الإجراءات المتاحة:** أزرار لـ "Redeem Code" (شحن كود)، "Activate Account"، "Extend Service" (تمديد الخدمة)، و "Change Service".

### نوع تسجيل الدخول والآلية:
*   هذا **ليس Captive Portal** (بوابة اعتراضية للوصول للإنترنت)، بل هي **بوابة إدارة مستخدمين (User Management Portal)** تعتمد على الويب.
*   التسجيل يتم عبر **واجهة ويب (Web Form)**. المتصفح يستخدم ملفات تعريف الارتباط (Cookies) أو بيانات الاعتماد المحفوظة لإتمام العملية. الموقع يتبع نظام "SAS RADIUS" من تطوير شركة "Snono Systems".

### ما ينبغي لتطبيق Android تقليده:
إذا كنت تهدف لبناء تطبيق Android يؤدي نفس الوظيفة، يجب عليه القيام بما يلي:
1.  **إدارة الجلسة (Session Management):** تخزين بيانات الاعتماد بشكل آمن (Encrypted SharedPreferences) واستخدامها لإرسال طلب `POST` إلى عنوان تسجيل الدخول الخاص بـ `admin.skylineiq.com`.
2.  **جلب البيانات (Data Fetching):** بدلاً من عرض صفحة الويب، يجب على التطبيق استخراج البيانات (Parsing) من استجابة الخادم (سواء كانت JSON عبر API إذا توفرت، أو HTML Scraping) وعرضها في واجهة Android أصلية (Native UI).
3.  **تنبيهات الحالة:** عرض الأيام المتبقية وحالة الاتصال (ONLINE) بشكل بارز في واجهة التطبيق.
4.  **تكامل الوظائف:** توفير أزرار برمجية تقوم بإرسال طلبات التفعيل أو تمديد الخدمة مباشرة من داخل التطبيق.
5.  **الأتمتة:** ميزة "تسجيل الدخول التلقائي" عند فتح التطبيق إذا كانت الجلسة لا تزال صالحة.