إليك التسلسل الزمني الكامل للمقطع، مع استخراج النصوص، تحليل آلية العمل، ومتطلبات التنفيذ البرمجية لنظام الأندرويد:

### أولاً: التسلسل الزمني واستخراج النصوص

**1. مرحلة الوصول (00:00 - 00:04):**
*   **الحدث:** يقوم المستخدم بفتح متصفح الإنترنت (Samsung Internet) وإدخال الرابط: `admin.skylineiq.com/user/#/login`.
*   **النصوص الظاهرة:** عناوين المواقع المقترحة، لوحة مفاتيح الأندرويد، وشريط العنوان.

**2. صفحة تسجيل الدخول (00:05 - 00:06):**
*   **العنوان الرئيسي:** SAS RADIUS.
*   **نصوص الحقول:**
    *   `Login` (Sign in to your account).
    *   `username or mobile number`.
    *   `your password`.
    *   قائمة اختيار اللغة (English).
*   **الأزرار:** `Login`, `Forgot password?`, `Register`.
*   **ملاحظة:** يظهر مؤشر تحميل (Spinner) في الثانية 00:06، مما يشير إلى محاولة تسجيل دخول تلقائية.

**3. لوحة تحكم المستخدم - User Portal (00:07 - 00:12):**
*   **المعلومات العامة (Account Information):**
    *   Remaining Days: `0 day(s)`.
    *   Account Balance: `IQD 0.00`.
    *   Unpaid Invoices: `0`.
*   **معلومات العميل (Customer Information):**
    *   ID: `300285`.
    *   Customer Name: `استاذ طه جديد`.
    *   Username: `bbn.17@haq2`.
*   **معلومات الخدمة (Service Information):**
    *   Current Service: `2-Nitro`.
    *   Subscription: `Active`.
    *   Expiration: `2026-09-13 23:07:38`.
    *   Status: `ONLINE`.
    *   Service Price: `IQD 45,000`.
*   **خيارات التحكم:** `Redeem Code`, `Activate Account`, `Extend Service`, `Change Service`.
*   **التذييل:** `SAS Radius © 2021 Snono Systems`.

---

### ثانياً: تحليل آلية تسجيل الدخول

**كيف تم تسجيل الدخول تلقائياً؟**
لم يقم المستخدم بإدخال البيانات يدوياً في الفيديو. حدث ذلك لأن المتصفح قام باستدعاء بيانات الاعتماد المخزنة مسبقاً (Saved Credentials) أو لوجود "جلسة" (Session) نشطة مخزنة في ملفات تعريف الارتباط (Cookies) أو التخزين المحلي (Local Storage) الخاص بالمتصفح. بمجرد تحميل الصفحة، قام كود JavaScript الخاص بالموقع بإرسال طلب المصادقة تلقائياً.

**هل هو Captive Portal أو API؟**
*   **الواجهة المعروضة:** هي **Web Portal (واجهة ويب)** وليست Captive Portal بالمعنى التقني الصرف (الذي يظهر عند الاتصال بشبكة Wi-Fi عامة).
*   **الآلية الخلفية:** الموقع يعتمد بالكامل على **API**. يظهر ذلك من هيكل الرابط (`/#/login`) الذي يشير إلى تطبيق صفحة واحدة (SPA) يستخدم Angular أو Vue.js، حيث يتواصل المتصفح مع خادم (Backend) عبر طلبات API لجلب بيانات المستخدم وعرضها ديناميكياً.

---

### ثالثاً: متطلبات تنفيذ تطبيق Android مشابه

لبناء تطبيق أندرويد يقوم بنفس الوظائف (إدارة اشتراك SAS Radius)، تحتاج إلى المتطلبات التالية:

**1. المتطلبات البرمجية (Technological Stack):**
*   **اللغة:** Kotlin أو Java.
*   **مكتبة الشبكات:** `Retrofit` أو `Volley` للتواصل مع REST API الخاص بنظام SAS Radius.
*   **إدارة البيانات:** `EncryptedSharedPreferences` لتخزين اسم المستخدم وكلمة المرور بشكل آمن لتسجيل الدخول التلقائي.

**2. المكونات الأساسية (Core Components):**
*   **Authentication Module:** للتعامل مع رموز الوصول (Tokens مثل JWT) والحفاظ على الجلسة نشطة.
*   **Dashboard UI:** استخدام `RecyclerView` أو `Cards` لعرض تفاصيل الاشتراك (الرصيد، تاريخ الانتهاء) كما هو ظاهر في الفيديو.
*   **WebView (اختياري):** إذا كنت تريد عرض بوابة الويب الأصلية داخل التطبيق، ولكن الأفضل برمجياً هو استخدام واجهات أصلية (Native UI) تتصل بالـ API.

**3. الأذونات (Permissions):**
*   `INTERNET`: للاتصال بالخادم.
*   `ACCESS_NETWORK_STATE`: للتحقق من وجود اتصال بالإنترنت قبل محاولة جلب البيانات.

**4. المتطلبات الأمنية:**
*   استخدام بروتوكول **HTTPS** حصراً لتشفير البيانات المرسلة.
*   تطبيق **Biometric Authentication** (بصمة الإصبع أو الوجه) قبل فتح لوحة التحكم لزيادة الأمان.